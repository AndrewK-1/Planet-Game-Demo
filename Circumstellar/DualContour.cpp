#pragma once
#include "DualContour.h"

/*
void DualContour::GenerateTriangle() {
	int x, y, z;
	if (z > 0) {
		if (x > 0 && y > 0 || x < 0 && y < 0) {
			triangle = this, x, y;
		}
		else {
			triangle = this, y, x;
		}
	}
	else {

	}
}
*/